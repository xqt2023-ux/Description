# ===========================================
# Description 视频编辑器 - 环境配置
# ===========================================

# -------------------------------------------
# 服务器配置
# -------------------------------------------
PORT=3001
NODE_ENV=development

# -------------------------------------------
# 数据库配置
# -------------------------------------------
# PostgreSQL 数据库连接（当前未使用，预留）
DATABASE_URL=postgresql://postgres:password@localhost:5432/descript_clone

# Redis 连接（用于任务队列，当前未使用，预留）
REDIS_URL=redis://localhost:6379

# -------------------------------------------
# 文件存储配置
# -------------------------------------------
UPLOAD_DIR=./uploads
EXPORT_DIR=./exports
MAX_FILE_SIZE=500MB

# -------------------------------------------
# AI 服务配置
# -------------------------------------------

# Anthropic Claude API（主要 AI 服务）
# 获取：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Groq API（免费快速的 Whisper 转录服务 - 推荐）
# 获取：https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# OpenAI API（备用 Whisper 转录和 GPT 服务）
# 获取：https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# -------------------------------------------
# 代理配置（可选）
# -------------------------------------------
# 如果您在企业网络环境或需要通过代理访问外部 API
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080

# 不需要代理的域名列表（逗号分隔）
# 注意：Edge TTS 的 WebSocket 连接不支持代理，已自动添加到白名单
# NO_PROXY=localhost,127.0.0.1,speech.platform.bing.com

# -------------------------------------------
# 身份认证（预留）
# -------------------------------------------
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# -------------------------------------------
# FFmpeg 配置
# -------------------------------------------
# Windows 默认路径（如果在 PATH 中，可以只写命令名）
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

# Linux/Mac 默认路径示例：
# FFMPEG_PATH=/usr/bin/ffmpeg
# FFPROBE_PATH=/usr/bin/ffprobe

# -------------------------------------------
# 日志和调试
# -------------------------------------------
# LOG_LEVEL=info
# DEBUG=false
